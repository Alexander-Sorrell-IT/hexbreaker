# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
dist/
build/
.venv/
venv/
env/

# Secrets / credentials
.env
.env.*
*.credentials.json
.credentials/

# Runtime outputs
transcripts/*.jsonl
transcripts/*.log
# Registry store — holds the WITHHELD seeds + answer keys; never commit (it would
# re-leak the sealed benchmark). registry_teardown.py regenerates it deterministically.
samples/registry_demo/registry.db
*.registry.db
docs/demo/_build/
cases/generated/
cases/starter/*.E01
cases/starter/*.dd
cases/starter/*.mem
cases/starter/*.bin
cases/starter/*.raw
cases/nist-hacking/*.E01
cases/nist-hacking/*.dd
*.E01
*.dd
*.raw

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Test artifacts
.pytest_cache/
.coverage

# Court run artifacts (regenerated by scripts; transcripts + sidecars + sigs)
_nist_court/
_verify_demo/
htmlcov/
.tox/

# Docker
.docker-build-cache/

# Local notes (planning lives outside the repo)
SCRATCH.md
TODO.local.md

# Internal planning / audit scratch + agent worktrees (keep OUT of the public repo)
.claude/
AUDIT_*.md
*_raw.json
MULTIFINDING_PLAN.md
RELOOK_STRATEGIC.md
ROADMAP_NEXT.md
